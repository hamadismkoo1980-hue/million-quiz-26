export async function onRequestPost({request,env}){const s=request.headers.get("x-admin-secret")||"";if(!env.ADMIN_SECRET||s!==env.ADMIN_SECRET)return j({ok:false,message:"غير مصرح"},401);const b=await request.json();const attempts=Math.max(1,Math.min(10,Number(b?.attempts||1)));const code="MQ-"+rand();const h=await sha(code);await env.DB.prepare("INSERT INTO vouchers(code_hash,attempts) VALUES(?,?)").bind(h,attempts).run();return j({ok:true,code,attempts})}function rand(){const b=new Uint8Array(6);crypto.getRandomValues(b);return [...b].map(x=>x.toString(16).padStart(2,"0")).join("").toUpperCase()}async function sha(s){const h=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(s));return [...new Uint8Array(h)].map(x=>x.toString(16).padStart(2,"0")).join("")}function j(o,s=200){return new Response(JSON.stringify(o),{status:s,headers:{"content-type":"application/json","cache-control":"no-store"}})}